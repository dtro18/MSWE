// import React from 'react';
// import ReactDOM from 'react-dom/client'; 
// import App from './main'; 

// const rootElement = document.getElementById('root') as HTMLElement; // Ensure 'root' matches the id in your HTML

// if (rootElement) {
//   const root = ReactDOM.createRoot(rootElement);

//   root.render(
//     <React.StrictMode>
//       <App />
//     </React.StrictMode>
//   );
// } else {
//   console.error("Root element not found!");
// }
